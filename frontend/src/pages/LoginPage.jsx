import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { api } from "../services/api";
import { useAuth } from "../context/AuthContext";
export function LoginPage(){const [form,setForm]=useState({login:"admin-ndmahsan",password:"Admin@12345"});const [error,setError]=useState("");const [loading,setLoading]=useState(false);const {login}=useAuth();const navigate=useNavigate();async function submit(e){e.preventDefault();setLoading(true);setError("");try{const r=await api.post('/auth/login',form);login(r.data.data);navigate(r.data.data.user.role==='SUPER_ADMIN'?'/super-admin':'/dashboard')}catch(err){setError(err.response?.data?.message||'Login failed')}finally{setLoading(false)}}return <div className="login-page"><form className="login-card" onSubmit={submit}><div className="login-logo">E</div><span className="eyebrow">ENTERPRISE ERP</span><h1>Secure Sign In</h1><p>Use your Super Admin or Institute account.</p>{error&&<div className="error">{error}</div>}<label>Username or email<input value={form.login} onChange={e=>setForm({...form,login:e.target.value})}/></label><label>Password<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><button className="primary" disabled={loading}>{loading?'Signing in...':'Sign In'}</button><div className="demo"><b>Super Admin:</b> admin-ndmahsan / Admin@12345<br/><b>Institute:</b> demo-admin / Institute@12345</div></form></div>}
